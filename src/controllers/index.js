const userController = require('./user');
const formController = require('./form');
const leadController = require('./lead');
const companyController = require('./company');
const formResponseController = require('./formResponse');

module.exports = {
  userController,
  formController,
  leadController,
  companyController,
  formResponseController,
};
