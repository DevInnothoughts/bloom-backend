const userController = require('./user');
const formController = require('./form');
const leadController = require('./lead');

module.exports = {
  userController,
  formController,
  leadController,
};
